<UserControl x:Class="App_Automatize_Backery.View.SupportControls.RawMaterialsWarehouseUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App_Automatize_Backery.View.SupportControls" xmlns:supportviewmodel="clr-namespace:App_Automatize_Backery.ViewModels.SupportViewModel" d:DataContext="{d:DesignInstance Type=supportviewmodel:RMWarehouseViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding WarehouseItems}"
                  AutoGenerateColumns="False"
                  Margin="10"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Сырья:" Binding="{Binding RawMaterialId}"/>
                <DataGridTextColumn Header="Название:" Binding="{Binding RawMaterial.RawMaterialName}"/>
                <DataGridTextColumn Header="Количество,кг|л:" Binding="{Binding RawMaterialCount}"/>
                <DataGridTextColumn Header="Поставка от:" Binding="{Binding DateSupplyOrProduction, StringFormat='dd.MM.yyyy HH:mm'}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
