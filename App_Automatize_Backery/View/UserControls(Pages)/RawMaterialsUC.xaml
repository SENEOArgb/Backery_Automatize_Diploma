<UserControl x:Class="App_Automatize_Backery.View.UserControls_Pages_.RawMaterialsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App_Automatize_Backery.View.UserControls_Pages_"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:viewmodels="clr-namespace:App_Automatize_Backery.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:RMViewModel}">

    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" Width="95" Height="40" VerticalAlignment="Top" >
            <Button Content="Создать" Command="{Binding CreateCommand}" Margin="5"/>
        </StackPanel>

        <DataGrid x:Name="rawMaterials" Grid.Row="1" ItemsSource="{Binding RawMaterials}" AutoGenerateColumns="False" 
                  HeadersVisibility="Column" CanUserAddRows="False" 
                  CanUserDeleteRows="False" CanUserResizeColumns="True"
                  Margin="0 70 0 0" Padding="10"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  SelectedItem="{Binding SelectedRawMaterial}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding RawMaterialName}" Width="Auto"/>
                <DataGridTextColumn Header="Срок годности (дни)" Binding="{Binding ShelfLifeDays, StringFormat={}{0} д.}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding RawMaterialCoast, StringFormat={}{0} руб.}" Width="80"/>
                <DataGridTextColumn Header="Ед. измерения" Binding="{Binding MeasurementUnit.MeasurementUnitName}" Width="120"/>

                <!-- Кнопка Редактировать -->
                <DataGridTemplateColumn Header="Редактировать">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="✎" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Кнопка Удалить -->
                <DataGridTemplateColumn Header="Удалить">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
