<UserControl x:Class="App_Automatize_Backery.View.UserControls_Pages_.UserUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App_Automatize_Backery.View.UserControls_Pages_" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:viewmodels="clr-namespace:App_Automatize_Backery.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Text="Текущий пользователь:" HorizontalAlignment="Center" FontSize="18" Margin="0 0 200 0" />
        <TextBlock Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding CurrentUser.FullName}"  HorizontalAlignment="Center" FontSize="18" Margin="0 0 200 0 "/>
        <Button Content="Выйти" VerticalAlignment="Top" HorizontalAlignment="Right" Grid.Column="1" Margin="0 0 60 0" Command="{Binding LogoutCommand}"/>
        <!-- Ежедневная норма (левая часть) -->
        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <materialDesign:Card Height="285" Padding="20" Background="LightGray">
                <StackPanel>
                    <TextBlock HorizontalAlignment="Center" Text="Ежедневная норма" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Margin="0 0 0 0" Text="Норма на день:" FontSize="14" />
                    <TextBlock Width="80" HorizontalAlignment="Right" Text="{Binding DailyNorm}" FontSize="16" FontWeight="Bold" Margin="0,-20,20,15"/>

                    <!-- Кнопка для изменения нормы -->
                    <Button Content="Задать норму" Command="{Binding SetDailyNormCommand}" HorizontalAlignment="Center" Margin="0,0"/>

                    <TextBlock Margin="0 10 0 0" Text="Произведено изделий:" FontSize="14" />
                    <TextBlock Width="80" HorizontalAlignment="Right" Text="{Binding DailyProduction}" FontSize="16" FontWeight="Bold" Margin="0,-20,20,15"/>

                    <TextBlock Margin="0 0 0 0" Text="Продано изделий:" FontSize="14"/>
                    <TextBlock Width="80" HorizontalAlignment="Right" Text="{Binding DailySale}" FontSize="16" FontWeight="Bold" Margin="0,-20,20,15"/>

                    <TextBlock Margin="0 0 0 10" Text="Выполнение нормы:" FontSize="14"/>
                    <ProgressBar Value="{Binding DailyCompletion}" Maximum="100" Height="10" 
                               Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,5,0,10"/>
                    <TextBlock Text="{Binding DailyCompletionText}" FontSize="14" FontWeight="Bold"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>

        <!-- Данные о пользователе (правая часть) -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20">
            <materialDesign:Card Padding="20" Background="LightGray">
                <StackPanel HorizontalAlignment="Center">
                    <!-- Фото пользователя -->
                    <Border Width="150" Height="150" CornerRadius="75" BorderBrush="Gray" BorderThickness="2">
                        <Image Source="\res\user_icon.png" Stretch="UniformToFill"/>
                    </Border>
                    <!-- Информация о пользователе -->
                    <TextBlock Text="Логин:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="{Binding CurrentUser.UserLogin}" FontSize="16"/>

                    <TextBlock Text="Роль:" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="{Binding CurrentUser.UserRoleName}" FontSize="16"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </Grid>
</UserControl>
