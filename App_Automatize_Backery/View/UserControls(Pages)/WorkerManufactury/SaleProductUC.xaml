<UserControl x:Class="App_Automatize_Backery.View.UserControls_Pages_.WorkerManufactury.SaleProductUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App_Automatize_Backery.View.UserControls_Pages_.WorkerManufactury"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <!-- Заголовок -->
            <TextBlock Text="Продукты в продаже:" FontSize="22" FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="470,0,0,10"/>

            <!-- Таблица продуктов -->
            <DataGrid ItemsSource="{Binding SaleProducts}" SelectedItem="{Binding SelectedSaleProduct}"
                      AutoGenerateColumns="False" IsReadOnly="True" RowHeight="40"
                      SelectionMode="Single" CanUserResizeRows="False" CanUserResizeColumns="False"
                      AlternatingRowBackground="{DynamicResource MaterialDesignPaper}">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Продукт" Binding="{Binding Product.ProductName}" Width="200"/>
                    <DataGridTextColumn Header="Количество,шт" Binding="{Binding CountProductSale}" Width="150"/>
                    <DataGridTextColumn Header="Стоимость" Binding="{Binding CoastToProduct, StringFormat={}{0} руб.}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
