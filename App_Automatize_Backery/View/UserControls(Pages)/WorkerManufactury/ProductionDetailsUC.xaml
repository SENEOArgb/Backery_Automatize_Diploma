<UserControl x:Class="App_Automatize_Backery.View.UserControls_Pages_.WorkerManufactury.ProductionDetailsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App_Automatize_Backery.View.UserControls_Pages_.WorkerManufactury"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Детали производства" FontSize="20" FontWeight="Bold" Margin="10" />

        <DataGrid ItemsSource="{Binding ProducedProducts}"
                  AutoGenerateColumns="False"
                  Margin="10,50,10,10"
                  IsReadOnly="True"
                  VerticalAlignment="Top"
                  Height="260"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="Auto" />
                <DataGridTextColumn Header="Количество" Binding="{Binding CountProduct}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
